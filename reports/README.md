# Reports

The original submission PDF is kept outside the source tree to avoid committing generated binary artifacts through routine pull requests.

The analytical results are documented in the root README, `MODEL_CARD.md`, and the reproducible notebook under `notebooks/`.
