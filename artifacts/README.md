# Model artifacts

Generated model bundles, metrics, and predictions are written here and ignored by Git.

Create a local model with:

```bash
python scripts/generate_demo_data.py
python scripts/train.py --input data/generated/demo_train.csv
```
