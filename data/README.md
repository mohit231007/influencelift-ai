# Data

- `sample/` contains synthetic public demonstration data.
- `raw/` is for local source datasets and is ignored by Git.
- `generated/` is created by `scripts/generate_demo_data.py` and is ignored by Git.
- `processed/` is reserved for local transformed outputs and is ignored by Git.

The original challenge files are intentionally not redistributed without an explicit dataset licence.
